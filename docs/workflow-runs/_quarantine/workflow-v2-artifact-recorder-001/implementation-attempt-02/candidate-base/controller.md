# v2 Controller

Keep v1 gates and role isolation. Invoke Recorder verify before transitions. Verify path, pair, length, SHA-256, sequence, attempt/revision and identity. Do not infer business correctness. Mismatch, secrets, missing raw export or invalid exception fails closed.
