# Manual handoff

Conversation copying is not raw export. Accept only platform export with provenance and attestation. Events 006/008 may use the explicitly approved encrypted-platform-record exception; mark plaintext unavailable and never claim semantic verification.
