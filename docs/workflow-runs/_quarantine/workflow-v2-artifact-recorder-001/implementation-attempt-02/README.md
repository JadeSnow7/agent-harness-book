# Invalid workflow-v2 candidates

This quarantine contains implementation-attempt-02 candidate-base and invalid-run bytes. They are invalid, not accepted, and the old verify result is untrusted. Inventory hashes are recomputed directly.
